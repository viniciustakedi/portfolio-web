/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'media.licdn.com', 
      'www.mundorh.com.br',
      'images.prismic.io',
      'img.freepik.com',
    ],
  },
  reactStrictMode: true,
}

module.exports = nextConfig
