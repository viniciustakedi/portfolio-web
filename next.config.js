/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'media.licdn.com', 
      'www.mundorh.com.br',
      'images.prismic.io',
    ],
  },
  reactStrictMode: true,
}

module.exports = nextConfig
